{
  "name": "my-naiscript",
  "description": "A NovelAI Script",
  "license": "MIT",
  "scripts": {
    "build": "nibs build",
    "typecheck": "tsc --noEmit",
    "format": "prettier -w .",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "nibs-cli": "^4.2.0",
    "prettier": "^3.7.0",
    "vitest": "^4.0.0"
  }
}
